import {AppBar} from "react-admin";
import {Typography} from "@mui/material";

export const MyAppBar = () => (
    <AppBar color="primary" position="sticky">
        Smilea dashboard system
    </AppBar>
);
